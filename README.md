# 微博助手与历史清理

一个基于 Chrome Manifest V3 的轻量扩展，包含两项核心能力：

- 微博发帖文本半自动填充
- 黑名单域名历史记录自动清理

## 功能说明

### 1. 历史记录自动清理

- 支持在弹窗中维护域名黑名单（增删改查）
- 切换标签页时，若命中黑名单域名，自动清理对应历史记录
- 支持“立即清理”按钮，手动触发一次全量清理
- 若后台连接异常，弹窗会自动降级为本地清理

### 2. 微博发帖辅助

- 仅在 `https://weibo.com/*` 注入内容脚本
- 监听微博编辑按钮点击，读取最新打卡文本并生成下一条内容
- 自动填充到发帖输入框（`textarea`）

## 安装方式（开发者模式）

1. 打开 `chrome://extensions`
2. 打开右上角“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择当前项目根目录
5. 每次代码改动后点击扩展卡片上的 `Reload`

## 使用说明

### 历史清理配置页

1. 点击浏览器工具栏扩展图标，打开弹窗
2. 在输入框中添加域名（如 `weibo.com`）
3. 可在列表中删除域名
4. 点击“立即清理”可立刻执行一次历史清理

### 微博辅助

1. 打开微博页面并进入发帖/编辑流程
2. 点击触发按钮后，扩展会尝试自动生成并填充文本

## 项目结构

- `manifest.json`：扩展声明与权限配置
- `js/background.js`：后台服务（黑名单同步、自动/手动历史清理）
- `js/popup.js`：弹窗逻辑（黑名单管理、立即清理）
- `popup.html`：弹窗结构
- `popup.css`：弹窗样式
- `js/content.js`：微博页面内容脚本（纯原生 JS，无 jQuery）
- `img/icon.png`：扩展图标

## 权限说明

- `tabs`：读取当前激活标签页 URL，用于判断是否命中黑名单
- `storage`：保存黑名单配置（`chrome.storage.sync`）
- `history`：搜索并删除命中域名的浏览历史

## 注意事项

- 当前微博辅助逻辑依赖页面 DOM 结构，若微博页面改版需同步调整选择器
- 历史清理使用域名匹配（支持子域名）
